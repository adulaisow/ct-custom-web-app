mutation UpdateCustomer($version: Long!, $id: String!, $actions: [CustomerUpdateAction!]!) {
  updateCustomer(version: $version, id: $id, actions: $actions) {
    id
    title
    firstName
    middleName
    lastName
  }
}